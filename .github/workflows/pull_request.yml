name: 'Pull Request'

on:
  pull_request:
    branches: [ develop ]
    types: [opened, reopened, synchronize, closed]

jobs:
  notify:
    if: >
      github.event.pull_request.state == 'open' || github.event.pull_request.state == 'closed' || github.event.pull_request.state == 'merged'
    runs-on: ubuntu-latest
    steps:
      - name: Set color_var
        run: echo "color_var=$(if [ ${{ github.event.pull_request.state }} == 'closed' ]; then echo '019128'; else echo 'ad020a'; fi)" >> $GITHUB_ENV
      - name: 📣 Send teams notification
        uses: simbo/msteams-message-card-action@latest
        with:
          webhook: ${{ secrets.TEAMS_WEBHOOK_URL }}
          color: ${{ env.color_var }}
          title: ${{ github.workflow }}
          message: 'status: <span style="color:#ffffff;background-color:#${{ env.color_var }};padding:3px;">${{ github.event.pull_request.state }}</span><hr style="height:2px;color:#fff;">'
          buttons: |
            View Pull Request ${{ github.event.pull_request._links.html.href  }}
            View Project https://github.com/orgs/uic-ts/projects/1
          sections: |
            -
              activityTitle:
              activitySubtitle:
              facts:
                -
                  name: "Repo:"
                  value: ${{ github.repository }}
                -
                  name: "Source branch:"
                  value: ${{ github.head_ref }}
                -
                  name: "Target branch:"
                  value: ${{ github.base_ref }}
                -
                  name: "Requested by:"
                  value: ${{ github.triggering_actor }}
                -
                  name: "PR #:"
                  value: ${{ github.event.pull_request.number }}
                -
                  name: "Review status:"
                  value: "`${{ github.event.pull_request_review.state }}`"
                -
                  name: "Reviewers:"
                  value: "
                    {% for reviewer in github.event.pull_request.requested_reviewers %}
                      {{ reviewer.login }}
                    {% endfor %}"
                -
                  name: "Commits in this PR:"
                  value: ${{ github.event.pull_request.commits }}
