name: 'Pull Request'

on:
  pull_request:
    branches: [ develop ]
    types: [opened, reopened, closed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Color variable conditions
        run: |
          if [[ "${{ github.event.pull_request.state }}" == "open" ]]; then
            echo "STATUS_COLOR=019128" >> $GITHUB_ENV
            echo "GREEN"
          fi
          if [[ "${{ github.event.pull_request.state }}" == "closed" ]]; then
            echo "STATUS_COLOR=ad020a" >> $GITHUB_ENV
            echo "RED"
          fi
          if [[ "${{ github.event.pull_request.merged }}" == "true" ]]; then
            echo "STATUS_COLOR=5a00ba" >> $GITHUB_ENV
            echo "PURPLE"
          fi
          echo "::set-env name=MESSAGE_TEXT::status: <span style=\"color:#ffffff;background-color:#${{ env.STATUS_COLOR }};padding:3px;\">${{ github.event.pull_request.merged && 'MERGED' || github.event.pull_request.state }}</span><hr style=\"height:2px;margin-top:7px;noshade;background-color:#${{ env.STATUS_COLOR }};\">"


      - name: 📣 Send teams notification for  pull request
        if: github.event.action == 'opened'
        uses: simbo/msteams-message-card-action@latest
        with:
          webhook: ${{ secrets.TEAMS_WEBHOOK_URL }}
          color: ${{ env.STATUS_COLOR }}
          title: ${{ github.workflow }}
          message: ${{ env.MESSAGE_TEXT }}
          buttons: |
            View Pull Request ${{ github.event.pull_request._links.html.href }}
            View org Project https://github.com/orgs/uic-ts/projects/1
          sections: |
            -
              activityTitle:
              activitySubtitle:
              facts:
                -
                  name: "Repo:"
                  value: ${{ github.repository }}
                -
                  name: "Source branch:"
                  value: ${{ github.head_ref }}
                -
                  name: "Target branch:"
                  value: ${{ github.base_ref }}
                -
                  name: "Author:"
                  value: ${{ github.triggering_actor }}
                -
                  name: "PR #:"
                  value: ${{ github.event.pull_request.number }}
                -
                  name: "Review status:"
                  value: "${{ github.event.pull_request_review.state }}"
                -
                  name: "Commits in this PR:"
                  value: ${{ github.event.pull_request.commits }}


