name: 'Pull Request'

on:
  pull_request:
    branches: [ develop ]
    types: [opened, reopened, closed]

jobs:
  notify:
    env:
      OPENED_GREEN: 019128
      CLOSED_RED: ad020a
      MERGED_PURPLE: 5a00ba
    runs-on: ubuntu-latest
    steps:

      - name: Color variable conditions
        run: |
          echo "Pull Request State: ${{ github.event.pull_request.state }}"
          if [[ "${{ github.event.pull_request.state }}" == "open" ]]; then
            echo "Setting STATUS_COLOR to OPENED_GREEN"
            echo "STATUS_COLOR=OPENED_GREEN" >> $GITHUB_ENV
          fi
          if [[ "${{ github.event.pull_request.state }}" == "closed" ]]; then
            echo "Setting STATUS_COLOR to CLOSED_RED"
            echo "STATUS_COLOR=CLOSED_RED" >> $GITHUB_ENV
          fi
          if [[ "${{ github.event.pull_request.state }}" == "merged" ]]; then
            echo "Setting STATUS_COLOR to MERGED_PURPLE"
            echo "STATUS_COLOR=MERGED_PURPLE" >> $GITHUB_ENV
          fi

      - name: 📣 Send teams notification for OPENED pull request
        uses: simbo/msteams-message-card-action@latest
        with:
          webhook: ${{ secrets.TEAMS_WEBHOOK_URL }}
          color: ${{ env.STATUS_COLOR }}
          title: ${{ github.workflow }}
          message: |
            status: 
            <span style="color:#ffffff;background-color:#${{ env.STATUS_COLOR }};padding:3px;">
              ${{ github.event.pull_request.state }}
            </span>
            <hr style="height:2px;margin-top:7px;noshade;
            background-color:#${{ env.STATUS_COLOR }};">
          buttons: |
            View Pull Request ${{ github.event.pull_request._links.html.href }}
            View org Project https://github.com/orgs/uic-ts/projects/1
          sections: |
            -
              activityTitle:
              activitySubtitle:
              facts:
                -
                  name: "Repo:"
                  value: ${{ github.repository }}
                -
                  name: "Source branch:"
                  value: ${{ github.head_ref }}
                -
                  name: "Target branch:"
                  value: ${{ github.base_ref }}
                -
                  name: "Author:"
                  value: ${{ github.triggering_actor }}
                -
                  name: "PR #:"
                  value: ${{ github.event.pull_request.number }}
                -
                  name: "Review status:"
                  value: "${{ github.event.pull_request_review.state }}"
                -
                  name: "Commits in this PR:"
                  value: ${{ github.event.pull_request.commits }}

